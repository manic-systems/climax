[package]
name                 = "pound"
version              = "0.2.0"
description          = "low footprint, derive-first CLI parser"
keywords             = [ "cli", "args", "argparse", "derive", "command-line" ]
categories           = [ "command-line-interface" ]
readme               = "../../README.md"
authors.workspace    = true
edition.workspace    = true
license.workspace    = true
repository.workspace = true

[features]
default = [ "std", "help", "derive" ]
std     = [  ]
help    = [ "pound-derive?/help" ]
derive  = [ "dep:pound-derive" ]

[dependencies]
pound-derive = { workspace = true, optional = true }

[[example]]
name              = "grab"
required-features = [ "std", "derive" ]

[[example]]
name              = "vault"
required-features = [ "std", "derive" ]

[[example]]
name              = "introspect"
required-features = [ "derive" ]
test              = true

[[example]]
name              = "command_docs"
required-features = [ "std", "help", "derive" ]
test              = true

[lints]
workspace = true
